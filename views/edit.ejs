<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Task</title>
    <style>
        /* General Reset */
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        /* Body and Layout */
        body {
            font-family: 'Arial', sans-serif;
            background-color: #282c34;
            color: #fff;
            padding: 0 20px;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            height: 100vh;
        }

        h1 {
            color: #ff5722;
            margin-bottom: 30px;
            font-size: 2.8rem;
            font-weight: 700;
        }

        /* Form Styling */
        form {
            background-color: #333;
            padding: 30px;
            border-radius: 10px;
            box-shadow: 0px 8px 20px rgba(0, 0, 0, 0.3);
            width: 100%;
            max-width: 450px;
            display: flex;
            flex-direction: column;
            gap: 20px;
        }

        /* Label Styling */
        label {
            color: #ff5722;
            font-size: 1.1rem;
            font-weight: 600;
        }

        /* Input Field Styling */
        input[type="text"],
        textarea {
            padding: 12px;
            border: 2px solid #444;
            border-radius: 6px;
            background-color: #555;
            color: #fff;
            font-size: 1rem;
            outline: none;
            transition: border-color 0.3s ease;
        }

        /* Styling for textarea */
        textarea {
            height: 120px;
            resize: none;
        }

        /* Input Focus Styles */
        input[type="text"]:focus,
        textarea:focus {
            border-color: #ff5722;
        }

        /* Button Styles */
        button {
            background-color: #ff5722;
            border: none;
            padding: 12px;
            border-radius: 6px;
            color: white;
            font-size: 1.1rem;
            font-weight: 600;
            cursor: pointer;
            transition: background-color 0.3s ease;
        }

        button:hover {
            background-color: #e64a19;
        }

        /* Responsive Styles */
        @media (max-width: 480px) {
            h1 {
                font-size: 2rem;
            }

            form {
                width: 90%;
                padding: 20px;
            }

            input[type="text"], 
            textarea {
                font-size: 0.9rem;
            }

            button {
                font-size: 1rem;
            }
        }
    </style>
</head>
<body>
    <h1>Edit Task</h1>

    <!-- Use form to submit the update -->
    <!-- Method Override: Use _method hidden field to turn the POST into a PUT request -->
    <form action="/todos/edit/<%= todo._id %>?_method=PUT" method="POST">
        <label for="title">Title:</label>
        <input type="text" id="title" name="title" value="<%= todo.title %>" required>
        
        <label for="description">Description:</label>
        <textarea id="description" name="description" required><%= todo.description %></textarea>
        
        <button type="submit">Update Todo</button>
    </form>
</body>
</html>
